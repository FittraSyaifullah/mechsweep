EXA_API_KEY=your_exa_api_key_here
EXA_BASE_URL=https://api.exa.ai
# auto (recommended), instant, fast, deep-lite, deep, deep-reasoning
EXA_SEARCH_TYPE=auto
EXA_NUM_RESULTS=100
# Optional: research paper, news, company, people, personal site, financial report
# EXA_CATEGORY=research paper
# Optional comma-separated domain allowlist (e.g. edu,gov sites)
# EXA_INCLUDE_DOMAINS=edu,ac.uk,gov
# Up to 1200 excluded domains derived from prior sweep/library URLs
# EXA_EXCLUDE_DOMAIN_LIMIT=1200
# Pipe-separated extra queries for deep search modes
# EXA_ADDITIONAL_QUERIES=heat transfer PDF|FEA textbook|machine design datasheet
# Results per batched /api/sweep call (max 100)
SWEEP_BATCH_SIZE=50
# Total unique results collected in one full sweep (batched calls)
SWEEP_SESSION_MAX=500
SWEEP_MAX_BATCHES=10
# Optional override for per-request Exa result count (max 100)
# SWEEP_MAX_RESULTS=100
SEARCH_PROVIDER=exa

MISTRAL_API_KEY=your_mistral_api_key_here
MISTRAL_BASE_URL=https://api.mistral.ai/v1
MISTRAL_ANALYZE_MODEL=mistral-small-latest
MISTRAL_SEARCH_MODEL=mistral-small-latest
MISTRAL_EMBEDDING_MODEL=mistral-embed

# Optional: set to true only if you want OpenRouter as fallback when Mistral fails
OPENROUTER_FALLBACK_ENABLED=false

OPENROUTER_API_KEY=your_openrouter_api_key_here
OPENROUTER_BASE_URL=https://openrouter.ai/api/v1
OPENROUTER_MODEL=google/gemini-2.5-flash-lite
OPENROUTER_ANALYZE_MODEL=google/gemini-2.5-flash-lite
OPENROUTER_SEARCH_MODEL=perplexity/sonar-pro
OPENROUTER_EMBEDDING_MODEL=openai/text-embedding-3-small

NEXT_PUBLIC_APP_URL=http://localhost:3000
